import { getCurrentYear } from '../utils';

export const CURRENT_YEAR: number = getCurrentYear() || 2025;

export const MINIMUM_PRODUCT_DELIVERY_TIME_IN_MINUTES: number = 1;
export const MAXIMUM_PRODUCT_DELIVERY_TIME_IN_MINUTES: number = 300;
